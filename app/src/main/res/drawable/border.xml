<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Relleno (opcional) -->
    <solid android:color="@color/celeste" /> <!-- Fondo blanco (opcional) -->

    <!-- Borde -->
    <stroke
        android:color="#000000"
        android:width="2dp"/>   <!-- Grosor del borde -->

    <!-- Radio de las esquinas (opcional) -->
    <corners android:radius="4dp" /> <!-- Esquinas redondeadas (opcional) -->

    <!-- Fondo (opcional) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" /> <!-- Padding (espaciado interno, opcional) -->
</shape>
